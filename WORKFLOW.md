# Tapmall 核心開發工作流 (WORKFLOW.md)

本文件定義了 Tapmall 專案的標準自動化開發與部署流程。未來所有的開發與操作皆須**嚴格遵守**此工作流程。

## 1. 智慧開發與自動除錯 (Dev & Auto-Debugging)
- **自動延伸必備功能**：在開發過程中，若發現需求有遺漏但「非常必要」的基礎功能（例如：實作購物車時發現缺少庫存檢查機制、或是使用者表單缺少了必要的驗證等），必須**主動延伸並實作**這些功能。
- **自動除錯與修復**：遇到程式碼報錯時，優先透過錯誤日誌自行排查並自動修復，確保在推送到遠端前沒有殘留的 Bug。
- **自動抓取網路圖片**：遇到需要商品圖片的情境（如資料庫 Seed 初始化、假資料生成等），一律自動整合開放 API (如 Unsplash / Picsum) 搭配 Faker.js，自動取得合法且高畫質的網路圖片，不再使用純文字或破圖。

## 2. 自動化測試 (Playwright)
為了確保產品穩定，我們導入雙重自動化測試防線：
1. **本機強制驗證 (AI Agent 階段)**：每次完成 UI 修改或核心邏輯後，在 Commit 之前，必須於本機自動執行 Playwright 端到端 (E2E) 測試。
2. **雲端阻擋機制 (GitHub Actions 階段)**：每次推送到 GitHub 時，CI/CD 腳本會自動在雲端重新執行 Playwright 測試。若測試失敗，則自動中斷部署程序。

## 3. 雙軌部署機制 (Nginx & GitHub Pages)
由於 GitHub Pages 只能展示前端靜態頁面，為了能完整測試前後端連線運作，本專案採取「本機 Nginx」與「雲端 GitHub Pages」並行部署：

- **本機/伺服器 Nginx (完整全端功能測試)**
  - 統一使用同一個 Port 開放對外連線。
  - **前端**：Nginx 將根目錄 (`/`) 導向至 `packages/web/dist`。
  - **後端 API**：Nginx 將所有 `/api` 開頭的請求透過 **反向代理 (Reverse Proxy)** 導向給背景執行的 NestJS 伺服器 (如 `localhost:3000`)。
  - 目的：完全避開 CORS 問題，並可透過這台機器的 IP 直接進行功能整合測試。

- **GitHub Pages (純前端 UI 預覽)**
  - 透過現有的 `deploy.yml` 持續由 GitHub Actions 自動發布。

## 4. 進階擴充自動化 (Advanced Workflows)
為了確保電商專案的穩定與未來高度擴充性，工作流中嚴格要求加入以下進階自動化機制：
- **程式碼風格自動化**：每次 Commit 前執行 `Prettier` 與 `ESLint`，自動排版並修復低級錯誤，確保程式碼品質完美。
- **API 文件自動化**：後端 API 修改時透過 Swagger / OpenAPI 自動產出最新文件，大幅降低前後端溝通成本。
- **資料庫遷移防呆**：部署前自動執行 Prisma Migrate Check，確保資料庫結構與程式碼絕對同步。
- **效能與無障礙檢測**：整合 Lighthouse / Web Vitals，在發布前自動化檢查前端效能與無障礙設計，確保最佳的購物體驗。
- **自動化文件與版本控制**：系統將依據開發進度自動更新 `README.md`，並整合自動化版本控制 (Versioning) 來管理專案的每一次迭代發布。
